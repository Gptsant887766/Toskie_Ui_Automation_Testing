# ── STAGE environment — overlay on top of base config.properties ──────────────
# Loaded when -Denvironment=stage or env var ENVIRONMENT=stage

environment=stage
baseUrl=https://stage.app.toskie.com/
apiUrl=https://stage-api.toskie.wasd.in/graphql
headless=true
max.retries=1
slowMo=0
page.load.timeout=45000
