# ── QA environment — overlay on top of base config.properties ────────────────
# Loaded when -Denvironment=qa or env var ENVIRONMENT=qa

environment=qa
baseUrl=https://qa.app.toskie.com/
apiUrl=https://qa-api.toskie.wasd.in/graphql
headless=true
max.retries=2
slowMo=0
page.load.timeout=30000
